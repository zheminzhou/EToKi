raxml={ETOKI}/externals/raxmlHPC                       # phylo
raxml_ng={ETOKI}/externals/raxml-ng                    # phylo
fasttree={ETOKI}/externals/FastTreeMP-DB               # phylo
rapidnj={ETOKI}/externals/rapidnj                      # phylo,ortho
bbduk={ETOKI}/externals/bbduk.sh                       # prepare
repair={ETOKI}/externals/repair.sh                     # prepare
bbmerge={ETOKI}/externals/bbmerge.sh                   # prepare
diamond={ETOKI}/externals/diamond                      # uberBlast
mmseqs={ETOKI}/externals/mmseqs                        # uberBlast
minimap2={ETOKI}/externals/minimap2                    # uberBlast,assemble-map
blastn={ETOKI}/externals/blastn                        # uberBlast
makeblastdb={ETOKI}/externals/makeblastdb              # uberBlast
spades={ETOKI}/externals/spades.py                     # assemble
megahit={ETOKI}/externals/MEGAHIT-1.2.9-Linux-x86_64-static/bin/megahit                      # assemble
samtools={ETOKI}/externals/samtools                    # assemble-map
nextpolish={ETOKI}/externals/nextpolish
hapog={ETOKI}/externals/hapog.py
bwa={ETOKI}/externals/bwa                              # assemble-map(optional)
bowtie2={ETOKI}/externals/bowtie2                      # assemble-map(optional)
bowtie2build={ETOKI}/externals/bowtie2-build           # assemble-map(optional)
kraken2={ETOKI}/externals/kraken2                      # assemble-eval
kraken_database={ETOKI}/externals/minikraken2          # assemble-eval
lastal={ETOKI}/externals/lastal                        # assemble-map
lastdb={ETOKI}/externals/lastdb                        # assemble-map
pilercr={ETOKI}/externals/pilercr                      # align-refmask
trf={ETOKI}/externals/trf409.linux64                   # align-refmask
flye=python {ETOKI}/externals/flye
usearch=/home/nigeldyer/CRobot/source/usearch/usearch11.0.667_i86linux32
treetime={ETOKI}/externals/treetime
